# Code review findings

Total: 7  ·  critical: 0  ·  high: 3  ·  medium: 1  ·  low: 3
Open: 7  ·  fixed: 0  ·  false-positive: 0  ·  wontfix: 0

## HIGH

### ResourceServer listens on all interfaces (0.0.0.0) — local network audio exposure — cpp:web/resource-server  (`f_20260623202035_4891`, open)

- path: `src/web/ResourceServer.cpp:42`
- evidence: soup_server_listen_all(server, 0, static_cast<SoupServerListenOptions>(0), &error)
- suggestion: Replace soup_server_listen_all with soup_server_listen_local to bind only to 127.0.0.1. No reason a local audio player needs to serve audio to the LAN.

### ResourceServer path traversal — any file readable via ../ in /audio/ URL — cpp:web/resource-server  (`f_20260623202035_2022`, open)

- path: `src/web/ResourceServer.cpp:96-107`
- evidence: auto *decoded = g_uri_unescape_string(p.c_str(), nullptr); std::string file_path = decoded ? decoded : p; ... std::ifstream file(file_path, std::ios::binary | std::ios::ate);
- suggestion: Resolve the path against a root directory and check that the resolved path starts with the root. Alternatively, require absolute paths (starting with /) and reject any path containing .. — since all audio paths come from FileScanner which returns absolute paths.

### Double-advance on track end when sorting is active — skips every other track — frontend:player-store  (`f_20260623202035_0786`, open)

- path: `frontend/src/stores/playerStore.ts`
- evidence: onAudioEvent("ended"): backend auto-advances in IPCHandler::audioEvent("ended"), then the frontend computes the next sorted track and calls playInFolder() again — advancing a second time.
- suggestion: Pick one owner for auto-advance: either disable the backend's auto-advance when sorting is active (check a flag), or let the frontend handle it exclusively and remove the auto-advance from IPCHandler::audioEvent when sorting is active. The sort-branch in onAudioEvent should NOT also call playInFolder — it should just sync state after the backend already advanced.

## MEDIUM

### MPRIS2 is a stub — no D-Bus object registered, update() is a no-op — cpp:mpris  (`f_20260623202035_7300`, open)

- path: `src/mpris/MPRIS2.cpp`
- evidence: MPRIS2::update(const PlayerState &state) { m_impl->last_state = state; } — just stores state, prints nothing, no D-Bus signals.
- suggestion: Either implement org.mpris.MediaPlayer2 on the session bus (with PropertiesChanged for track metadata, play/pause/next/prev/seek methods), or remove the class entirely if MPRIS2 is not needed yet. Currently it wastes a constructor call and an update() call per state change.

## LOW

### Global mutex in IPCHandler is unnecessary — saucer exposes run serially on one thread — cpp:ipc  (`f_20260623202035_5569`, open)

- path: `src/ipc/IPCHandler.cpp:16`
- evidence: static std::mutex s_state_mtx; // ponytail: single lock, per-command if contention
- suggestion: The global mutex is never contended (Saucer's exposed functions execute on the main thread). Remove it to simplify the code and avoid a false sense of thread-safety.

### Icon written to disk on every startup — unnecessary I/O — cpp:tray  (`f_20260623202035_7295`, open)

- path: `src/tray/SystemTray.cpp:303-312`
- evidence: g_file_set_contents(icon_file.c_str(), reinterpret_cast<const char *>(s_tray_icon_data), s_tray_icon_size, nullptr); — called unconditionally on every app start.
- suggestion: Check if the file exists and has the correct size first. Or write a stamp file. Avoids unnecessary disk writes on every launch.

### App.css contains unused saucer boilerplate — dead code embedded in binary — frontend:assets  (`f_20260623202035_8662`, open)

- path: `frontend/src/App.css`
- evidence: File contains .counter, .hero, #center, #next-steps, etc. — none of these selectors are used in any component. main.tsx only imports index.css.
- suggestion: Delete frontend/src/App.css — it's from the saucer template and no component references it.

